# URL of the Deuna realtime backend. In local dev point this at the
# Fastify server inside `deuna-api/` (default port 4000). In Vercel
# set it to the Fly.io URL (https://deuna-api.fly.dev).
NEXT_PUBLIC_API_URL=http://localhost:4000
